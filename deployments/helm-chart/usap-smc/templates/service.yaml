apiVersion: v1
kind: Service
metadata:
  name: {{ include "usap-smo.fullname" . }}
  labels:
    {{- include "usap-smo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "usap-smo.selectorLabels" . | nindent 4 }}
