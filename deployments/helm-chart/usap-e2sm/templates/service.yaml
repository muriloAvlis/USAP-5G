apiVersion: v1
kind: Service
metadata:
  name: {{ include "usap-e2sm.fullname" . }}
  labels:
    {{- include "usap-e2sm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "usap-e2sm.selectorLabels" . | nindent 4 }}
