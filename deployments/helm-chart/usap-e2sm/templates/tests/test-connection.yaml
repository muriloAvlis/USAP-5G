apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "usap-e2sm.fullname" . }}-test-connection"
  labels:
    {{- include "usap-e2sm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "usap-e2sm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
