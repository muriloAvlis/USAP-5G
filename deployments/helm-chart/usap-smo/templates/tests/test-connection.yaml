apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "usap-smo.fullname" . }}-test-connection"
  labels:
    {{- include "usap-smo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "usap-smo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
